const sendNotification = async (userId, title, message) => {
    // Logic for database or push notifications
    console.log(`Notification to ${userId}: ${title}`);
};

module.exports = { sendNotification };
